#flashcards
***
У его [[Пространства имен (namespaces)]] (тоже ранее говорилось) есть интерфейс [[docker0]] и eth0 ([[Virtual Ethernet Pair (Veth)]]), который выступает в качестве [[Конечная точка (Endpoint)]].
- Хост и [[Контейнер]] соединены парой ***veth***.
- Весь трафик проходит через интерфейс eth0 и направляется к соответствующим контейнерам через docker0.
