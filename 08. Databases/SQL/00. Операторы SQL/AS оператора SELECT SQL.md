#flashcards/databases   
***
Переименовывает столбцы в выводе.
- Меняет название только на экране, в самой базе столбец не переименовывается.
- Используется вместе с [[SELECT оператор SQL|SELECT]].
***
***Пример.***
```SQL
SELECT name AS "Имя клиента", age AS "Возраст" FROM person;
```