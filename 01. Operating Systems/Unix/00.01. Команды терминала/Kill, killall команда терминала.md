#flashcards/os/Unix 
***
`KILL` Убивает (т.е. завершает) [[Процесс|процесс]].
Нужно обладать правами [[Суперпользователь (root)|суперпользователя]], чтобы посылать сигналы процессам, которыми не владеем.
***Синтаксис.***
```bash
kill <номер PID>
```
У команды есть сигналы, каждый из которых отвечает за свою работу `kill`. Выводится через `kill -l`.
***
`KILLALL` позволяет посылать сигналы нескольким процессам, соответствующим указанной программе или имени пользователя.
***
***Пример.***
Используем [[Перевод процессов в фоновый режим|перевод в фоновый режим]].
```bash
xlogo &
xlogo &
killall xlogo
```
